# 複製為 .env.local 後依環境調整。
# 注意：NEXT_PUBLIC_ 前綴會被打包進前端 bundle（瀏覽器可見），
# 僅放「本就公開」的值（如 API base URL）；機密請留在後端，切勿放這裡。
NEXT_PUBLIC_API_BASE=http://localhost:5001/api/v1

# 選填：設定後 Profile 頁顯示 Google 登入按鈕（需與後端 GOOGLE_CLIENT_ID 一致）
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=xxxx.apps.googleusercontent.com
